import React from 'react';
import styled from 'styled-components';

const Container = styled.div`
  max-width: 490px;
  border-left: 1px #ddd solid;
`

const TagsList: React.FC = () => {
  return (
    <Container>TagsList</Container>
  )
}

export default TagsList